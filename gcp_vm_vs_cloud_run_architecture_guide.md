# GCP Compute Engine (VM) vs Cloud Run 하드웨어 및 아키텍처 비교 가이드

본 문서는 Google Cloud Platform(GCP)에서 웹 애플리케이션을 배포할 때 가장 대표적으로 비교되는 **Compute Engine (가상 머신)**과 **Cloud Run (서버리스 컨테이너)**의 하드웨어 리소스 할당 메커니즘, 디스크 동작 원리, 비용 구조 및 아키텍처 차이점을 알기 쉽게 정리한 종합 가이드입니다.

---

## 💡 1. 한눈에 이해하는 핵심 비유

* **Compute Engine (VM 인스턴스)**:
  * **"24시간 통째로 빌려둔 오피스텔(원룸)"**
  * 손님이 오든 안 오든, 내가 자고 있든 깨어 있든 **24시간 내내 월세와 전기세가 청구**됩니다.
  * 가구 배치, 청소, 보일러 수리(OS 보안 패치, Nginx 웹서버, SSL 인증서 등)를 **세입자(개발자)가 직접** 해야 합니다.
* **Cloud Run (서버리스 컨테이너)**:
  * **"필요할 때만 호출해서 타는 우버/택시"**
  * 손님이 타서 목적지까지 이동하는 **그 시간(초 단위)에만 요금이 발생**합니다.
  * 손님이 없으면 대기 차량이 **0대**로 쉬고 있으므로 **비용이 0원($0)**입니다.
  * 자동차 정비나 주유(서버 관리, HTTPS 보안 인증서 발급, 인프라 패치)는 **구글이 100% 대신** 해줍니다.

---

## ⚙️ 2. 하드웨어 리소스 할당 방식의 근본적 차이

### ① CPU & 메모리(RAM) 결정 방식

| 구분 | Compute Engine (VM) | Cloud Run (서버리스) |
| :--- | :--- | :--- |
| **할당 단위** | **컴퓨터 본체 1대** 전체 | **컨테이너 1대** 단위 |
| **결정 방식** | 구글이 정해둔 **머신 등급** 선택<br/>*(예: `e2-medium` = 2 vCPU, 4GB RAM)* | 개발자가 필요한 수치를 **자유롭게 직접 입력**<br/>*(예: `--cpu=1`, `--memory=512Mi`)* |
| **기본값** | 별도 기본값 없이 머신 패밀리 필수 선택 | 미지정 시 **1 vCPU / 512MiB** 자동 적용 |
| **설정 가능 범위** | 머신 유형에 따라 고정된 비율 조합 | **CPU**: 1, 2, 4, 8 vCPU<br/>**RAM**: 128MB ~ 최대 32GB까지 유연한 조합 |
| **스펙 변경** | 서버를 일시 중단(Stop)한 후 수동 변경 | 콘솔 클릭 한 번 또는 명령어 1줄로 즉시 재배포 |

---

### ② 스토리지(디스크 용량)의 동작 원리 (★ 핵심 차이점)

#### 1) Compute Engine (VM): 영구 하드디스크 (Persistent Disk)
* 물리적인 컴퓨터 본체에 SATA/NVMe SSD를 장착한 것과 같습니다.
* **영구 보관(Persistent)**: 서버를 재부팅하거나 전원을 껐다 켜도 저장된 파일, 데이터베이스, 로그가 **그대로 유지**됩니다.
* **용량 설정**: 인스턴스 생성 시 `10GB`, `100GB` 등 물리적 디스크 용량을 직접 지정합니다.

#### 2) Cloud Run: 무상태(Stateless) & 인메모리 가상 디스크
* Cloud Run 컨테이너에는 기본적으로 **영구적인 하드디스크가 존재하지 않습니다.**
* **인메모리 임시 파일시스템 (tmpfs)**:
  * 컨테이너 내부(예: `/tmp`)에서 파일을 생성하거나 다운로드하면, 하드디스크가 아니라 **컨테이너에 할당된 메모리(RAM)의 일부를 쪼개서 가상 디스크**로 사용합니다.
* **휘발성(Stateless)**:
  * 접속자가 없어서 컨테이너가 0대로 축소(종료)되면, 컨테이너 내부 파일시스템에 썼던 파일은 **자동으로 깨끗이 삭제**됩니다.

> 💡 **Cloud Run에서 영구 데이터(사용자 업로드 사진, 첨부파일 등)는 어떻게 보관하나요?**
> * 영구 보관이 필요한 파일은 구글 클라우드의 오브젝트 스토리지인 **Google Cloud Storage (GCS 버킷)**에 업로드하여 보관합니다.
> * 필요 시 Cloud Run 컨테이너에 GCS 버킷을 마치 로컬 폴더처럼 연결하는 **볼륨 마운트(Volume Mount)** 기능을 활용할 수도 있습니다.
> * 정형 데이터는 **Cloud SQL (MySQL/PostgreSQL)** 등 외부 데이터베이스에 저장합니다.

---

### ③ 인스턴스 자동 확장 (Autoscaling) 메커니즘

* **Compute Engine (VM)**:
  * 기본적으로 컴퓨터 **1대 고정**입니다.
  * 접속자가 1명이든 10,000명이든 동일한 컴퓨터 1대가 버텨야 하므로, 트래픽 폭증 시 서버가 다운될 수 있습니다.
* **Cloud Run**:
  * 컨테이너 대수가 **고무줄처럼 실시간 자동 증감**합니다:
    ```bash
    --min-instances=0    # 평소 손님이 없을 땐 0대 (비용 $0)
    --max-instances=3    # 트래픽 폭증 시 최대 3대까지 자동 복제
    ```
  * 손님이 0명일 때는 0대 ➔ 손님이 오면 `1 vCPU / 512MB` 컨테이너가 1대 가동 ➔ 손님이 몰리면 2대, 3대로 자동 복사되어 트래픽 분산 ➔ 손님이 나가면 다시 0대로 내려갑니다.

---

### ④ CPU 작동 모드 (비용 절감의 비밀)

Cloud Run은 CPU 작동 모드를 선택할 수 있어 비용을 극단적으로 아낄 수 있습니다:

1. **요청 처리 중에만 CPU 할당 (기본값, CPU only allocated during request processing)**:
   * 사용자가 질문을 전송하고 답변이 브라우저로 전송되는 **그 몇 초 동안만 CPU가 켜지고 요금이 계산**됩니다.
   * 대기 중(유휴 상태)에는 CPU가 꺼지므로 비용이 청구되지 않습니다.
2. **항상 CPU 할당 (CPU always allocated)**:
   * 백그라운드 상시 작업이나 WebSocket 연결 유지가 필요한 경우 VM처럼 CPU를 상시 켜둘 수도 있습니다.

---

## 📊 3. 종합 비교 요약표

| 비교 항목 | Compute Engine (VM 인스턴스) | Cloud Run (서버리스 컨테이너) |
| :--- | :--- | :--- |
| **기본 아키텍처** | IaaS (인프라 서비스) | Serverless CaaS (컨테이너 서비스) |
| **하드웨어 설정** | 고정 머신 타입 (`e2-medium` 등) | 컨테이너당 CPU/RAM 직접 입력 (`1 vCPU`, `512MB`) |
| **디스크(Storage)** | 영구 Persistent Disk (10GB~수 TB) | 무상태 휘발성 (RAM 가상 디스크, 영구 저장은 GCS/DB 활용) |
| **비용 청구 기준** | 24시간 상시 과금 (월 ~$25~) | **실제 요청 처리 시간만 초 단위 과금 (미사용 시 $0)** |
| **무료 혜택** | 상시 무료 VM 스펙 제한적 | **매월 200만 건 요청 및 36만 GB-초 무료** |
| **HTTPS 인증서** | Let's Encrypt / Certbot 수동 발급 및 갱신 | **구글 관리형 글로벌 HTTPS 자동 발급 (`*.run.app`)** |
| **도메인 연결** | 고정 IP 할당 및 와일드카드 DNS(`sslip.io`) 필요 | **고유 보안 서브도메인 즉시 제공** |
| **오토스케일링** | 수동 인스턴스 그룹 구성 필요 | **0개부터 자동 확장 (0 ➔ N대 ➔ 0대)** |
| **적합한 서비스** | 상시 실행 백그라운드 워커, DB, 레거시 시스템 | **웹 챗봇, REST API, 웹훅, 마이크로서비스, 웹 프론트엔드** |

---

## 🛠️ 4. 우리 챗봇 프로젝트 실제 배포 스펙 비교

| 구분 | 1. 어제 배포한 Compute Engine | 2. 오늘 배포한 Cloud Run |
| :--- | :--- | :--- |
| **서비스명** | `chatbot-instance` | `gemini-chatbot` |
| **리전** | `us-central1-a` | `us-central1` |
| **할당 스펙** | 2 vCPU, 4GB RAM, 10GB SSD | **1 vCPU, 512MB RAM** |
| **비용 구조** | 켜두는 동안 매월 약 3~4만 원 고정 발생 | **접속자가 없을 때 인스턴스 0대 유지 (비용 0원)** |
| **접속 주소** | `https://34.63.148.160.sslip.io` (Nginx 프록시) | `https://gemini-chatbot-976675812314.us-central1.run.app` |
| **Secret 연동** | startup-script로 Secret Manager 조회 후 파일 생성 | 배포 시 `--set-secrets`로 환경변수 즉시 주입 |

---

## 🎯 5. 실무 선택 가이드 (언제 무엇을 써야 할까?)

* **Cloud Run을 선택해야 하는 경우 (적극 권장)**:
  * 웹 챗봇, 포트폴리오, 블로그, API 백엔드처럼 **사용자가 24시간 내내 계속 들어오지 않는 서비스**.
  * 서버 OS 보안 업데이트나 Nginx 설정 등에 신경 쓰지 않고 **코드 개발에만 집중하고 싶을 때**.
  * 테스트나 개인 프로젝트 유지 비용을 **0원($0)**으로 만들고 싶을 때.

* **Compute Engine (VM)을 선택해야 하는 경우**:
  * 24시간 내내 메모리에 데이터를 올려두고 멈추지 않고 돌아야 하는 대규모 배치 서버.
  * MySQL, Oracle 등 직접 OS에 설치하여 운영해야 하는 자체 데이터베이스.
  * 특수한 리눅스 커널 모듈이나 네트워크 인터페이스 제어가 필요한 레거시 기업용 시스템.
